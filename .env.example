# Backend origin reached by the dev/preview server proxy (vite.config.js).
# Deliberately has NO VITE_ prefix: Vite never inlines it into the client bundle.
BACKEND_ORIGIN=https://<backend-host>

# Must match RESPONSE_ENCODE_SALT on the server
VITE_RESPONSE_SALT=KXXADFDFDF
